# ---------- secrets ----------
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ---------- python ----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/

# ---------- node ----------
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
.eslintcache
playwright-report/
test-results/
blob-report/

# ---------- e2e scratch ----------
# Playwright starts the API from the repo root with FHE_DATA_DIR=.e2e-data,
# so the throwaway database lands here rather than under apps/web.
/.e2e-data/

# ---------- data (never commit bulk provider dumps) ----------
data/raw/
data/cache/
data/warehouse/
data/imports/*
!data/imports/.gitkeep
!data/imports/README.md
*.parquet
*.duckdb
*.sqlite
*.sqlite3
*.db
# fixtures are the deliberate exception: small, sanitized, committed on purpose
!data/fixtures/**

# ---------- ml artifacts ----------
ml/artifacts/
ml/datasets/
*.pkl
*.joblib
!ml/registry/**/*.json

# ---------- os / editor ----------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# ---------- claude local ----------
.claude/settings.local.json

# ---------- next.js build output ----------
apps/web/.next/
apps/web/out/

# Regenerated by create-next-app; the project's own CLAUDE.md is at the root.
apps/web/CLAUDE.md
apps/web/AGENTS.md
